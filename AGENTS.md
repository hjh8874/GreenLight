# 공통 작업 규칙

## 답변 스타일

- 한국어로 답변한다.
- 먼저 결론을 짧게 말한다.
- 이후 필요한 세부 설명을 붙인다.
- 설명은 짧은 문장 여러 개로 나눈다.
- 질문이 여러 개이면 번호를 맞춰서 답변한다.
- 표처럼 가로 폭이 넓은 형식은 피한다.
- 여러 파일 경로는 한 줄에 하나씩 작성한다.

## 코드 답변 규칙

- 코드를 바로 많이 던지기보다
  왜 그렇게 하는지 먼저 짧게 설명한다.
- 사용자가 “전체 코드”, “수정본”, “붙여넣을 코드”라고
  요청했을 때만 완성 코드를 제공한다.
- 사용자가 “힌트만”, “일단 인지만”,
  “답 알려주지 말고”라고 하면 구현 코드를 제공하지 않는다.
- 코드 설명은 가능하면
  “짧은 코드 조각 → 바로 아래 설명” 형태로 작성한다.
- 실제 프로젝트 파일에는 `// [add]`, `// [change]`, `// [edit]`처럼
  작업 표시용 주석을 남기지 않는다.
- 변경 표시가 필요하면 답변 설명에서만 구분하고,
  커밋 대상 코드에는 기능 설명에 필요한 주석만 남긴다.

## PR 설명 작성 규칙

- 사용자가 PR 설명, PR 본문, PR에 적을 내용 작성을 요청하면
  아래 템플릿 형식을 사용한다.
- 각 항목은 짧고 명확하게 작성한다.
- 변경 이유, 실제 변경 내용, 확인 방법, 위험 요소를 분리해서 적는다.
- Scene, Prefab, Meta, Package 변경 여부는 반드시 Yes / No로 표시한다.
- 리뷰어가 특히 봐야 할 부분은 Reviewer Notes에 따로 적는다.

```md
## Purpose
- 왜 이 변경이 필요한가?

## Changes
- 무엇을 바꿨는가?

## Check
- 어떻게 확인했는가?

## Risk
- Scene 변경: Yes / No
- Prefab 변경: Yes / No
- Meta 변경: Yes / No
- Package 변경: Yes / No

## Reviewer Notes
- 리뷰어가 특히 봐야 할 부분
```

## 로컬 파일 확인 규칙

- 스크립트에 대해 답변하기 전에는
  관련 로컬 파일을 다시 읽고 현재 내용을 기준으로 답변한다.
- 이전 대화나 기억만으로 코드 내용을 단정하지 않는다.
- 답변 전에 rg로 관련 파일과 사용 위치를 찾는다.
- 필요한 파일은 Get-Content로 다시 확인한다.
- 파일을 직접 읽지 못한 경우에는
  “현재 로컬 파일 기준”이라고 말하지 않는다.

## 파일 수정 규칙

- 사용자가 명시적으로 직접 수정/적용을 요청하기 전까지
  실제 프로젝트 파일은 수정하지 않는다.
- 리뷰, 설명, 예시 코드, 전체본 요청은
  파일 수정 요청으로 간주하지 않는다.
- 직접 수정한 뒤에는
  어떤 파일의 어느 위치를 왜 수정했는지 설명한다.
- 직접 수정한 뒤에는
  실제로 변경된 코드 조각을 보여준다.

## 원본 보존 규칙

- 기존 코드 스타일을 최대한 유지한다.
- 기존 주석, 빈 줄, 섹션 구분, Header, Tooltip을
  임의로 삭제하거나 요약하지 않는다.
- 기존 주석이 오래되었거나 부정확해 보여도
  임의로 지우지 않는다.
- 기존 코드를 대체해서
  “깔끔한 새 전체본”으로 갈아엎지 않는다.
- 원본 구조를 유지한 채 필요한 줄만 추가/수정한다.

## Unity / C# 공통 규칙

- SerializeField로 연결된 필드명 변경은 주의한다.
- 필드명을 바꿔야 할 경우
  FormerlySerializedAs 사용을 고려한다.
- public API, Inspector 연결, Prefab/Scene 참조를
  함부로 깨지 않는다.
- Rigidbody, Collider, Trigger, LayerMask, Raycast 사용 시
  Unity 물리 흐름을 고려한다.
- Input System이나 Input Manager 변경은
  사용자가 명시적으로 요청하지 않으면 진행하지 않는다.
- Scene 오브젝트 구조 변경이 필요해 보여도
  코드 수정만으로 해결 가능한지 먼저 검토한다.

--- project-doc ---

# GreenLight 프로젝트 규칙

## 프로젝트 개요

- Unity 6 / C# 기반 3D 액션 RPG 학습용 프로젝트다.
- 이 프로젝트는 2차 프로젝트 기준이다.
- 1차 프로젝트 cmt_gun은 참고 자료로만 사용한다.
- 답변은 먼저 2차 프로젝트의 현재 구조를 확인한 뒤 작성한다.

## 작업 가능 영역

- 작업은 현재 GreenLight 프로젝트 안에서만 진행한다.
- 사용자가 지정한 2차 프로젝트 작업 폴더를 우선한다.
- 사용자가 명시적으로 요청하지 않는 한
  Packages, ProjectSettings, 외부 에셋 폴더,
  플러그인 폴더는 수정하지 않는다.
- 프로젝트 안에 참고용 폴더가 있다면
  해당 폴더는 읽기 전용으로 취급한다.

## 1차 프로젝트 참고 규칙

- 1차 프로젝트 스크립트는 참고 자료로만 사용한다.
- 1차 프로젝트 코드를 2차 프로젝트에
  그대로 복사하지 않는다.
- 먼저 2차 프로젝트 구조에 맞는지 확인한다.
- 답변할 때는 가능하면 아래처럼 구분한다.

```text
- 1차 프로젝트에서 그대로 참고 가능한 부분
- 2차 프로젝트에 맞게 수정해야 하는 부분
- 2차 프로젝트에서는 사용하지 않는 게 좋은 부분
```

## 코드 확인 규칙

- 스크립트 답변 전에는 rg로 관련 파일과 사용 위치를 찾는다.
- 필요한 파일은 Get-Content로 다시 확인한다.
- 1차 프로젝트 기억만으로 2차 프로젝트 구조를 단정하지 않는다.
- 현재 파일을 읽지 못했다면
  현재 로컬 파일 기준이라고 말하지 않는다.

## 수정 규칙

- 사용자가 직접 수정/적용을 요청하기 전까지
  실제 프로젝트 파일은 수정하지 않는다.
- 기존 동작을 크게 바꾸는 리팩터링은
  먼저 설명하고 확인을 받은 뒤 진행한다.
- 2차 프로젝트의 기존 구조를 우선한다.
- 1차 프로젝트 구조를 억지로 이식하지 않는다.

## 설계 비교 방식

- 먼저 2차 프로젝트 현재 구조를 확인한다.
- 그 다음 1차 프로젝트 구현과 비교한다.
- 재사용 가능한 개념과
  새로 맞춰야 하는 구현을 분리해서 설명한다.
- 기능 제안은 현재 프로젝트 규모에 맞게 작게 시작한다.

## Unity 구현 기준

- Rigidbody, Collider, Trigger, LayerMask, Raycast 사용 시
  Unity 물리 흐름을 고려한다.
- 입력 처리, 카메라, 이동, 공격, 상호작용,
  인벤토리 관련 변경은 영향 범위를 먼저 설명한다.
- Scene 오브젝트 구조 변경이 필요해 보여도
  코드 수정만으로 해결 가능한지 먼저 검토한다.
