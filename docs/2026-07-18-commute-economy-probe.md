# 통근 배정 ↔ 게임 경제 연결 — 진단 & 제안 (프로브, 2026-07-18)

> 코드 무변경 조사 + 특성화 테스트. 경제는 준희 오너 영역 — 이건 **제안**이지 확정 변경 아님.
> 브랜치 `feat-commute-economy-probe-hwan` (car-sim HEAD 위). 테스트만 커밋, 로직 변경 0.

## 현재 구조 (코드 사실)
| 레이어 | 파일 | 하는 일 |
|---|---|---|
| 배정 | `DemandMap.AssignType` | 각 집 → **같은 도로섬 최근접 사무실**(맨해튼), `DemandChoicePool`(3) K-최근접 해시 택1, sticky, 사무실당 `OfficeCapacity`(20) 캡 |
| 실현 | `CommuteScheduler.Rebuild` | 배정 짝 중 `OfficeParkingSlots`(6)만 WorkSlot 얻어 실제 통근 |
| 코인 | `CarSim.Step` → `ArrivalEvent` → `WeeklyEconomyLoopService.OnArrival` | 회사 도착 1회 = flat `CoinPerTrip`(10) → 주간 pending → 수동 수확 |
| (+보너스) | `DistanceRewardService` | 도착 목적지 평균 경로거리 비례 보너스 코인. **이미 배선·배포씬 라이브.** |

## 진단 — 구조적 긴장 3가지
1. **최근접 배정 = 거리 최소화 = 정체 최소화.** 코어 루프가 "교통 최적화로 돈"인데, 배정이 통근거리를 구조적으로 짧게 만들어 **최적화할 정체가 잘 안 생긴다** → 플레이어 실력이 개입할 여지 약화.
2. **코인이 flat.** 긴/어려운 통근을 성사시켜도 보상 동일 → 도로·신호 투자 유인이 약함. `DistanceRewardService`가 바로 이 공백을 메우려 존재.
3. **캡 불일치 (테스트로 확인).** `OfficeCapacity`(배정 20) vs `OfficeParkingSlots`(통근 6)이 서로 다른 레이어에 살고 대화하지 않음.
   - `CommuteEconomyProbeTests` (2/2 PASS): 30집→1사무실 ⇒ **통근 6대·하루 최대 60코인**, 배정 용량 20은 코인에 **무영향(inert)**. 코인 실질 레버 = **사무실 주차슬롯 총합**.

## 제안 (택1/조합 — 준희 판정)
- **A. 거리보상 정식 채택**: `DistanceRewardService`를 "차=돈 1:1 위반"이 아니라 **코어 경제 다리**로 승격. 긴 통근 성사 = 더 많은 코인 → 도로 최적화가 곧 수입. (이미 구현돼 있어 최소 변경.)
- **B. 중력(gravity) 배정**: 최근접 대신 `attractiveness / distance^p`. 고가치 사무실이 먼 통근을 끌어 정체 유발 → 최적화 여지 + 사무실 배치가 공간·경제 퍼즐. (더 큰 기획 변경.)
- **C. 캡 통일**: `OfficeCapacity` ↔ `OfficeParkingSlots`를 하나로, 또는 슬롯을 도로 처리량에 연동해 **사무실 수입 = 실제 도착 처리량**. 레버를 명확히.

## 추천
경제 다리(DistanceReward)는 이미 있음. **최소 변경 = A(거리보상 유지·정식화) + C(캡 통일)** 로 "정체를 뚫을수록 돈"을 성립시키고, B(중력 배정)는 W-단위 그레이박스 검증 후 도입 판단.

## 미결 (환/준희)
- DistanceReward 존치 여부 (앞선 ① PR #110 에스컬레이션과 동일 항목) — A안이면 존치가 답.
- 캡 통일 시 밸런스 재튜닝 (사무실 1개 하루 수입 상한 = 슬롯 × CoinPerTrip).

## 프로브 산출물
- `Assets/Tests/EditMode/CommuteEconomyProbeTests.cs` — 캡 결합 수치 고정, EditMode 2/2 PASS (Unity 6000.5.2f1).

---

# 차량 이동 자연스러움 — 감사 & 수정 로그 (2026-07-18)

환 라이브 피드백(빠름·뚝뚝 끊김·교차로에서 사라짐·회사 앞 충돌/플래쉬)에서 출발.
3-에이전트 병렬 감사 + Unity 런타임 계측(`execute_code`)으로 원인을 수치 확정하고 수정.
**모두 3차 빌드(차Sim 전면 교체) 시점부터 있던 기존 결함** — 되돌려도 남는 것들이었다.

## 수정 4건 (전부 뷰/기하, Sim 무변경)

| # | 커밋 | 원인 | 계측 결과 |
|---|---|---|---|
| B | `6900ee6` | 코너 분기가 `if`/`else if`라 첫 후보가 코너가 아니면 폴백 없이 직선으로 떨어짐. RF가 `[0.6,0.85]`로 클램프돼 두 창이 **항상 겹쳐** 상시 발동 | 우회전 타일별 이동 **1.0/0.697/1.018/1.0 → 1.0/1.012/1.012/1.0**(대칭), 위치 스텝 max/mean **1.000**(불연속 제거) |
| A | `8afc5df` | 주차 정착 속도를 **경로 전체 길이**로 계산 → 20타일 경로면 프레임당 3.7타일, 남은 스퍼를 1프레임에 주파 | `parkingSettleSeconds`(0.3s) 연출이 **처음으로 실제 실행**됨 |
| C | `7b73283` | 큐 표시 범위 `inset 0.25 + gap 0.4×3 = 1.45타일` > 타일 1.0 → 뒷차가 두 타일 뒤에 그려지고 1.2타일 점프 | 범위 **1.45 → 1.00타일**(담김), 교차로 속도계단 **1.66 → 1.27배** |
| — | `b3d235f` | 틱 보간이 항상 `SmoothStep` → t=0·1에서 미분 0이라 **매 타일마다 정지·재출발** | 순항 속도 변동 **13.1배 → 1.3배** |

`b3d235f`의 해법: 전 구간 선형(맥동은 없으나 출발·정지가 딱딱)도, 전 구간 이징(맥동)도 아닌
**가감속이 실제로 일어나는 순간에만 이징** — `PolylineMath.TickEase(t, cruising)`.
순항 = 선형(등속), 정지에서 출발·대기 진입·상태 전이 = SmoothStep.

**결과: TickInterval을 올려 속도를 낮춰도 맥동이 생기지 않는다** (이게 속도를 못 낮추던 이유였음).

## 남은 항목 (환 결정 필요)
1. **속도** — `SimConfig_Integrated.asset`의 `TickInterval` 0.1 → 0.3~0.4. 씬이 쓰는 건 `_Integrated` 쪽(`SimConfig.asset`은 씬 참조 0회). 이제 올려도 안 끊긴다.
2. **잔여 겹침** — `QueueCapacityPerTile=4`는 기하학적 과포화(차 길이 0.38~0.44 × 4 = 1.5타일 > 1.0). 클램프해도 실효 간격 0.293이라 겹침이 남음. **3으로 낮추면 간격 0.44로 완전 해소**(도로 처리량 감소 = 밸런스 결정).
3. **교차로 잔여 계단 1.27배** — 완전 해소는 틱 위상 보간 대신 **등속 리쉬**(뷰가 일정 속도로 목표를 추격)로 가야 함. 뷰가 Sim을 약간 지연 추적하는 트레이드오프.
4. **`MaxStepsPerFrame` 캡** 걸리면 `TickProgress01`이 1.0에 고정돼 보간 정지 → 렉 시 순간이동.

## 되돌린 시도 (기록)
- 캡 통일(일자리=통근) → 주차 슬롯이 20이 되며 도로 위 주차·플래쉬. **경제 일자리 수와 뷰 주차 슬롯은 분리해야 함**(한 타일에 20대는 기하학적으로 불가).
- 리빌드 연속성(건설 시 전 차량 스냅 제거) → 재인큐가 인덱스 순서라 차끼리 통과, 재인큐 실패 시 집으로 순간이동. 물리적 순서 정렬이 선행돼야 함.
