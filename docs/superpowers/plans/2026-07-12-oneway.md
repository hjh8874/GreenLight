# 일방통행 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 도로 타일 일방통행 배치 — 라우팅 간선 필터(상태 확장 없음), 세이브(첫 방향 필드), 화살표 마커 + 역주행 유령 숨김, 샌드박스 키.

**Architecture:** 스펙 `docs/superpowers/specs/2026-07-12-oneway-design.md`의 결정 9개가 규범. 좌표→방향 Dictionary + flat List(신호 가족 패턴, 단 값이 있어 셋 공통화 비대상). 라우팅은 Search 이웃 확장의 3규칙 필터.

**Tech Stack:** Unity EditMode NUnit + Unity MCP. TDD 필수(각 태스크 RED 확인).

## Global Constraints

- 브랜치 `feat-oneway-hwan`(스택: feat-simconfig-seam-hwan 위). 커밋 접두 `[Feat]`.
- 결정론: flat(y,x) 정렬 순회·세이브 순서, Dictionary는 조회만. 틱 중 할당 금지(재계획 경로는 허용).
- 라우팅 필터 3규칙(스펙 §핵심결정): 나가는 스텝=D만 / 들어가는 스텝≠−D(측면 합류 허용) / 일방 관여 대각 금지.
- 배치 조건: 배치 모드 && 도로 && `!IsIntersection` && 미배치 && dir은 4방 단위벡터.
- 기존 스위트 163 그린 유지. Unity MCP: refresh(force)→CS 0→run_tests job 폴링. Thread.Sleep 금지.

---

### Task 1: 배치 API + 세이브 (엔진 상태)

**Files:** Modify `Sim/SimEngine.cs`(가족 4번째 — 필드·API·프루닝·스냅샷), `Contracts/ISignalControl.cs`(4종+GetOnewayDir 제안, "공통 계약 분리 격상" 주석), Create `Contracts/Save/OnewaySaveData.cs`, Modify `Contracts/Save/SimSaveData.cs`. Test: `Assets/Tests/EditMode/OnewayTests.cs`(신규).

**Interfaces (Produces):** `IReadOnlyList<Vector2Int> OnewayTiles` / `bool CanPlaceOneway(Vector2Int)` / `bool TryPlaceOneway(Vector2Int, Vector2Int dir)` / `bool TryRemoveOneway(Vector2Int)` / `Vector2Int GetOnewayDir(Vector2Int)`(없으면 `Vector2Int.zero`) / 내부 `Dictionary<Vector2Int,Vector2Int> _onewayDirs` + `List<Vector2Int> _placedOneways`(flat 정렬). Task 2가 `_onewayDirs`를 Plan에 전달.

- [ ] TDD: 스펙 §4의 1·5·6번 테스트 먼저(RoundaboutTests.Build 직선 기하 재사용 — 직선 도로 타일이 배치 대상, 교차로 (3,0)·(6,0)은 거부 핀에 사용) → RED(CS) → 구현 → 그린.
- [ ] 배치/철거 시 `_grid.MarkTopologyDirty()` 호출(라우팅 영향 — 신호 가족과 다른 점, 주석 명기).
- [ ] 프루닝: RebuildSignals 소멸 블록 4번째 — 조건 `GetTile==Road && !IsIntersection` 위반 시 소멸.
- [ ] 스냅샷: `Oneways`(X,Y,DirX,DirY) — 복원은 배치 모드 블록 4번째, 배치 조건 재검증(손상 세이브 방어) + flat Sort.
- [ ] 전체 EditMode 그린 → 커밋 `[Feat] 일방통행 배치 API + 세이브 — 도로 전용, 첫 방향 필드`

### Task 2: 라우팅 간선 필터

**Files:** Modify `Sim/RoutePlanner.cs`(Search 이웃 확장), `Sim/SimEngine.cs`(Plan 호출에 `_onewayDirs` 전달). Test: OnewayTests 추가.

**Interfaces (Consumes):** Task 1의 `_onewayDirs`. **Produces:** `RoutePlanner.Plan(..., IReadOnlyDictionary<Vector2Int,Vector2Int> oneways)` — 기존 시그니처는 null 위임(기존 테스트 무수정 생존).

- [ ] TDD: 스펙 §4의 2(우회 라우팅 — 평행 2본 기하, RoutePlannerTests의 우회 기하 패턴 참조)·3(미도달 무사고)·4(합류 허용/역주행 금지 구분) RED 확인 → 필터 3규칙 구현 → 그린.
- [ ] 필터는 이웃 후보 루프 안 조기 continue — 일방 0개면 Dictionary 조회 없이 스킵(null/Count==0 가드, 무비용 경로).
- [ ] 결정론 테스트 + 전체 회귀 → 커밋 `[Feat] 일방통행 라우팅 — 간선 필터 3규칙(상태 확장 없음)`

### Task 3: 뷰 + 샌드박스 키

**Files:** Modify `View/MainCityView.cs`(RefreshOneways 화살표 마커 + MoveVehicle 역주행 숨김), `Debug/SandboxPlacementControls.cs`(4키 = 배치/방향회전, 패널 갱신).

- [ ] 마커: 삼각 화살(Cube 2개 조합 또는 Quad 회전 — 기존 CreateSignalBar류 재사용), `GetOnewayDir`로 회전. 수명 = 기존 폴링 규약.
- [ ] 유령 숨김: 복귀 구간 && insideTile 일방 && travelDir·D < −0.9 → `Renderer.enabled=false`(부속 마커 포함), 조건 해제 시 복원.
- [ ] 샌드박스 `4`: 호버 도로에 배치(E 시작), 재입력 시 방향 회전(철거→재배치로 구현), 패널에 방향 표시.
- [ ] Play 스모크(샌드박스 씬): 배치→화살표 마커 확인→역방향 수요 우회 확인(DeliveredTotal 유지)→`0` 철거. config 원복·워킹트리 클린.
- [ ] 전체 회귀 → 커밋 `[Feat] 일방통행 뷰 — 화살표 마커 + 역주행 유령 숨김 + 샌드박스 키`

## 완료 기준

- EditMode 163 + 신규(~10) 전부 그린, Play 스모크 통과, 최종 브랜치 리뷰(fable) "Ready to merge", 푸시 + 스택 PR.
